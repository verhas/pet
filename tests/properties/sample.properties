# Application configuration
server.host=localhost
server.port=8080

! database settings
db.url=jdbc:postgresql://localhost/mydb
db.username=admin
db.password=secret

app.name=my-app
app.version=1.0.0

# colon separator is also valid
log.level: INFO
log.file: /var/log/app.log
